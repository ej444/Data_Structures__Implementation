/* Linked List collection in Java

// Importing libraries
import java.util.LinkedList;

// Initializing a LL
List<Integer> llist = new LinkedList<Integer>();

// LL methods
llist.add("item") >> To add a new item
llist.get(n) >> To get the nth(starting from 0) element of the LL

llist.addFirst("first_item")/llist.addLast("last_item") >> Adds the node at the starting/ending of LL
llist.deleteFirst()/llist.deleteLast() >> Deletes the node at the starting/ending of LL
 * add() and addLast() do the same thing *

llist.add(position, "item") >> Adds the items at a  given position
llist.delete(position) >> Deletes the node at the given position

// Display the whole LL
System.out.println(llist)

 */

import java.io.*;
import java.util.List;
import java.util.LinkedList;

public class Merge
{
    public static void main(String[] args)
    {
        LinkedList<Integer> llist = new LinkedList<Integer>();
        llist.add(3);
        llist.add(2);
        llist.add(1);
        llist.add(6);
        llist.add(5);
        llist.add(4);
        
        
    }

}


















